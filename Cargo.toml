[package]
name = "dgmark"
version = "0.1.0"
edition = "2018"

[dependencies]
nom = "7.0.0"
serde = { version = "1.0.130", features = ["derive"] }
quick-xml = { version = "0.22.0", features = ["serialize"] }

[dev-dependencies]
criterion = "0.3.5"

[[bench]]
name = "document"
harness = false
